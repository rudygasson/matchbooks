
<h2><%= @deliverer.username %></h2>

<h3>Schedule a meeting at</h3>
<h2><%= @location.name %></h2>
<%= simple_form_for @meeting do |f| %>
  <%= f.input :date, as: :date, html5: true %>
  <%= f.input :time, as: :time, html5: true %>
  <%= f.submit "Request", class: "btn btn-flat-add" %>
<% end %>
